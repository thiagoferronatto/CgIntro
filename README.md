# A Better 3D Engine Than VBAG

No documentation for now. Currently, `scene.cpp` is really cluttered and
contains most of the code for actually building the OpenGL app and GUI; should
fix that sometime.